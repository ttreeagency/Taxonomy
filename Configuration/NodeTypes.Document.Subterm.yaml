'Ttree.Taxonomy:Document.Subterm':
  label: "${q(node).property('title') + ' (' + q(node).closest('[instanceof Ttree.Taxonomy:Document.Term]').first().property('title') + ')'}"
  superTypes:
    'Ttree.Taxonomy:Mixin.Term': true
  constraints:
    nodeTypes:
      'Neos.Neos:Document': false
      'Ttree.Taxonomy:Document.Term': false
      'Ttree.Taxonomy:Document.Subterm': true
  ui:
    label: 'Subterm'
